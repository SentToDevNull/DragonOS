# The URL at which the new patch can be found

https://cdn.kernel.org/pub/linux/kernel/v4.x/patch-4.11.8.xz

# The `md5sum` of the patch; also used to determine PATCH_OLDNAME

fca793663570ab8849ac3930b909ba0a  patch-4.11.8.xz

# PATCH_NEWNAME (for renaming, since many are just named 'patch')

linux-4.11.8.patch

# The command to be applied to the downloaded file; can rename file

xz -dc "$PATCH_OLDNAME" > "$PATCH_NEWNAME" && rm -f "$PATCH_OLDNAME"
